{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "npm run build --workspace @ifyrt/contracts && npm run build --workspace @ifyrt/service-core && npm run build --workspace @ifyrt/simulation-core && npm run build --workspace @ifyrt/sim-worker",
    "watchPatterns": [
      "package.json",
      "package-lock.json",
      "tsconfig.base.json",
      "apps/sim-worker/**",
      "packages/contracts/**",
      "packages/service-core/**",
      "packages/simulation-core/**"
    ]
  },
  "deploy": {
    "startCommand": "node apps/sim-worker/dist/index.js",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 120,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
