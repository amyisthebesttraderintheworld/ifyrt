{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "deploy/railway/n8n.Dockerfile",
    "watchPatterns": [
      "deploy/railway/n8n.Dockerfile",
      "n8n/workflows/**",
      "supabase/schema.sql"
    ]
  },
  "deploy": {
    "startCommand": "sh -lc 'export N8N_PORT=${PORT:-5678}; export N8N_PROTOCOL=https; export N8N_HOST=${N8N_HOST:-${RAILWAY_PUBLIC_DOMAIN}}; export WEBHOOK_URL=${WEBHOOK_URL:-https://${N8N_HOST}}; export N8N_EDITOR_BASE_URL=${N8N_EDITOR_BASE_URL:-https://${N8N_HOST}}; n8n start'",
    "healthcheckPath": "/healthz",
    "healthcheckTimeout": 180,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
